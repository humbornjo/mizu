version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - goconst
    - misspell
    - gocyclo
    - gosec
    - testpackage
    - tparallel
  disable:
    - goconst
